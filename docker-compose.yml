version: '3.8'

services:
  api-gateway:
    build:
      context: ./api-gateway
    ports:
      - "8080:8080"

  service-registry:
    build:
      context: ./service-registry
    ports:
      - "8761:8761"

  user-service:
    build:
      context: ./user-service
    ports:
      - "8081:8080"
